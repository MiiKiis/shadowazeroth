/**
 * @deprecated Este archivo es un remanente de BlizzCMS.
 * El proyecto usa pure Next.js con AzerothCore directamente.
 * Usa los pools de '@/lib/db' en su lugar:
 *   - pool      → characters (acore_characters)
 *   - authPool  → auth (acore_auth)
 *   - worldPool → world (acore_world)
 */

export {};
